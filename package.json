{
  "name": "job-finder-app",
  "version": "1.0.0",
  "private": true,
  "description": "Personal NZ tech intern/grad job alert tool — scrapes job boards, filters by location/keyword, dedupes, and emails a daily digest.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "digest": "node scripts/run-local.js",
    "digest:dry": "node scripts/run-local.js --dry-run"
  },
  "dependencies": {
    "mongodb": "^6.9.0",
    "resend": "^4.0.1"
  }
}
